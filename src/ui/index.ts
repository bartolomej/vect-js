import Button from "./button";
import Controls, { ControlsProps } from "./controls";
import Input, { InputProps, InputType } from './input';

enum ControlsType {
  BUTTON,
  INPUT
}

export { Button, Input, Controls, InputType, InputProps, ControlsProps, ControlsType};